interface:
  display_name: "MUSF Server Ops"
  short_description: "Operate MUSF LAN server, GM, FileServer, and health checks"
  default_prompt: "Start, stop, validate, and recover the MUSF LAN server stack."
