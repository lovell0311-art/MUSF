interface:
  display_name: "MUSF Map Regression"
  short_description: "Validate MUSF map, minimap, NPC, and scene bundle alignment"
  default_prompt: "Check MUSF map positioning, minimap scaling, and scene bundle consistency."
